// The channels the app starts with, as a list of kick.com names (the part that
// comes after kick.com/ in the address). It ships empty on purpose: a fresh
// install shows a short "add a channel" screen and you build the list from
// inside the app, which saves it on the TV. Pre-fill this array if you want
// some defaults instead.
var SEED_FAVORITES = [
];
