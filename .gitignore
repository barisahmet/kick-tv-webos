# macOS
.DS_Store

# Built packages
*.ipk

# Node
node_modules/
npm-debug.log*

# Private notes / rules (device IPs, personal channel data, etc.)
CLAUDE.md

# Local dev / deploy tooling (CDP scripts, Node shim) — not for the public repo
tools/


# Design docs and implementation plans — kept local, not for the public repo
docs/
